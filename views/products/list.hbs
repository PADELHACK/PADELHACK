<link rel="stylesheet" href="/styles/list.css">


{{!-- <div class="row row-cols-1 row-cols-md-2 row-cols-lg-5 g-4">
  {{#each products}}
    <div class="cz">
      <div class="card">
        <a href="/products/detail/{{_id}}"><img src="{{image}}" class="card-img-top" alt="imagen de detalle"></a>
        <div class="card-body">
          <h5 class="card-title">{{name}}</h5>
          <h6 class="card-subtitle mb-2 text-body-secondary">{{price}} €</h6>
          <h6 class="card-subtitle mb-2 text-body-secondary">{{brand}}</h6>
          <p class="card-text">{{shortDescription}}</p>
          <a href="/products/detail/{{_id}}" class="btn btn-primary">See more details</a>
        </div>
      </div>
    </div>
  {{/each}}
</div> --}}

	<div class="wrap">
{{#each products}}
		<div class="tarjeta-wrap">
			<div class="tarjeta">
				<div class="adelante card1">
          <a href="/products/detail/{{_id}}">
            <img src="{{image}}" class="card-img-top" alt="imagen de detalle">
          </a>
        </div>
				<div class="atras">
					        <div class="card-body">
          <h5 class="card-title">{{name}}</h5>
          <h6 class="card-subtitle mb-2 "><strong>{{price}} €</strong></h6>
          <h6 class="card-subtitle mb-2 ">{{brand}}</h6>
          <p class="card-text">{{shortDescription}}</p>
          <a href="/products/detail/{{_id}}" class="btn btn-primary">Mas detalles</a>
        </div>
				</div>
			</div>
		</div>
{{/each}}
  </div>