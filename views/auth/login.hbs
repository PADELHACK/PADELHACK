<h1 class="mt-2 ms-5">Inicia sesión en PADELHACK</h1>

<form action="/login" method="POST" class="mt-3 ms-5">
  <div class="col-md-3">
    <label for="email" class="form-label">Email address</label>
    <input
      class="form-control{{#if errors.email}} is-invalid{{/if}}"
      name="email" type="email" id="email" placeholder="name@example.com" value="{{user.email}}" required
    >
    {{#if errors.email}}
      <div class="invalid-feedback">
        {{errors.email}}
      </div>
    {{/if}}
  </div>
  <div class="col-md-3 mt-3">
    <label for="password" class="form-label">Password</label>
    <input
      class="form-control" 
      name="password" type="password" id="password" required
    >
  </div>
  <div class="flex mt-3">
    <button class="btn btn-primary" type="submit">Sign in</button>
    <a href="/login/google" class="btn btn-danger">Sign in with Google</a>
  </div>
</form>